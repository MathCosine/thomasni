# Copy this file to ".env" and adjust as needed before running.

# Public base URL of the site (used for the RSS feed and canonical links).
SITE_URL=http://localhost:3000

# Optional: override where the hearts/comments SQLite file is stored.
# Defaults to ./data/blog.db
# DATABASE_PATH=/data/blog.db
